import * as React from 'react'
import SimpleTable from './index'

export default {
  title: '@daimre-ui/SimpleTable',
  component: SimpleTable,
}

export const Basic = (args) => <SimpleTable {...args} />

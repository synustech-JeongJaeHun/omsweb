import * as React from 'react'
import Loader from './index'

export default {
  title: '@synusdev-ui/Loader',
  component: Loader
}

export const Basic = (args) => <Loader {...args} />
Basic.args = {}
